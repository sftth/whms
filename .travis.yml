language: java
jdk:
  - openjdk15

before_install:
  - chmod +x ./mvnw

branches:
  only:
    - dev-v1

# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.mvnw'
script: "./mvnw clean package"

notifications:
  email:
    recipients:
      - sftth@naver.com